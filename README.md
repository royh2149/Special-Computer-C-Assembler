# Special-Computer-C-Assembler
This is a C program that receives as command-line arguments a list of .as files, and compiles them: creates an object file (.ob), an entry file (.ent), and an externals file (.ext).
